numbers = (1, 2, 3) + (4, 5, 6)
print(numbers) # Devuelve 1, 2, 3, 4, 5, 6

point = tuple([1, 2]) # Convertimos una lista de [1, 2] a una tupla
print(point) # Devuelve (1, 2)

print(numbers[:2])  # Devuelve una tupla con lo elementos dede el índice 0 al índice 2, 2 exclusivo


